{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Major creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('major') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
