{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Requirements</h1>
    {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-default" href="{{ path('page_new') }}">
            New Page
        </a>
    {% endif %}

    <table class="table">
        <thead>
        <tr class="sr-only">
            <th>Title</th>
            <th>Body</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('page_show', { 'id': entity.id }) }}">{{ entity.title }}</a></td>
                <td>{{ entity.body|slice(0, 100)| raw }}. . .{% if is_granted('ROLE_ADMIN') %}<a href="{{ path('page_edit', { 'id': entity.id }) }}">edit</a>{% endif %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
