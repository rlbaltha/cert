{% extends '::base.html.twig' %}

{% block body -%}
<div class="col-sm-12">
    <h1>Responses</h1>
    {% for response in entity.responses %}
        <div class="card card-block">
            {{ response.question.question |raw }}<br/>
            {{ response.response|raw }}

        </div>

    {% endfor %}

    </div>
{% endblock %}
